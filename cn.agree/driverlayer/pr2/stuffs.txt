package print

对保存打印内容的xml，生成dom树

目前是两种模式，预计会有三种模式
1.table嵌套，在固定的行距下，把全页划分为table row和table date cell，定位准确，代码简单，但适应性差
2.rect定位，直接标注各个对象的坐标，然后从上到下，对每一行进行组织，最后打印
3.对象顺序打印，使用pr2的上下左右都可以移动的特点，按照对象的顺序，计算路径，依次打印